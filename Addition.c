#include <stdio.h>
int main()
{

  int a1, b1,c, sum;

  printf("Enter three numbers to add\n");
  scanf("%d%d", &a1, &b1, &c);

  sum = a1 + b1 + c;
  printf("Sum of the numbers = %d\n", sum);

  return 0;
}

add=a+b;
